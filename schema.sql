CREATE TABLE Student (
    roll_no INT PRIMARY KEY,
    name VARCHAR(50),
    course_name VARCHAR(50),
    m1 INT,
    m2 INT,
    m3 INT,
    total INT,
    average DOUBLE,
    result VARCHAR(10)
);